<IfModule mod_rewrite.c>
    RewriteEngine on

    RewriteBase /remix/demo/public/
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    #RewriteCond %{REQUEST_FILENAME} ^(.*)$
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>
