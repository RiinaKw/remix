#!/usr/bin/env php
<?php

// Be careful of directory settings
$app_dir = __DIR__ . '/app';
$project_dir = __DIR__;

require_once($project_dir . '/../vendor/autoload.php');

use Remix\Audio;

Audio::debug();
Audio::getInstance()
    ->daw
    ->initialize($app_dir)
    ->playCli($argv);
